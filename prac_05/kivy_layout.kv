<MyRoot>:
    orientation: "vertical"
    padding: 10
    spacing: 10

    # The existing status_text area
    Label:
        id: status_text
        text: "Status: Ready"
        size_hint_y: 0.2  # Takes up 20% of the vertical space

    BoxLayout:
        id: names_box
        orientation: "vertical"
        size_hint_y: 0.7  # Takes up 70% of the vertical space

    # 新增 Label
    Label:
        text: "I did this :)"
        size_hint_y: 0.1  # Takes up 10% of the vertical space
